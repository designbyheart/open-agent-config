---
label: Swift / Vapor
---

## Stack: Swift / Vapor

- Fluent filter syntax: `.filter(\.$field, .equal, value)` — never `==`.
- `swift build` must pass before finishing.
- Know whether the test DB uses migrations or a schema SQL file before changing models.
